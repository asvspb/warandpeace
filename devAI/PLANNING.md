# План портирования улучшений из `develop` в `main`

**Общая цель:** Упростить архитектуру ветки `main`, повысить ее надежность и удобство сопровождения за счет переноса ключевых улучшений из ветки `develop`.

---

### 1. Рефакторинг основной логики и базы данных (Выполнено)

-   **[X] Задача:** Заменить сложный трехэтапный конвейер в `src/bot.py` на единую, простую задачу `check_and_post_news`.
-   **[X] Действие:**
    -   Переписать `src/bot.py`, объединив логику.
    -   Упростить `src/database.py`, убрав систему статусов и лишние таблицы.
-   **[X] Обоснование:** Кардинальное снижение сложности, повышение предсказуемости и надежности.

---

### 2. Оптимизация развертывания и конфигурации (Выполнено)

-   **[X] Задача:** Упростить конфигурацию Docker и обработку API-ключей.
-   **[X] Действие:**
    -   Объединить `healthcheck` в основной сервис бота.
    -   Изменить `src/config.py` для чтения ключей из одной переменной `GOOGLE_API_KEYS`.
-   **[X] Обоснование:** Упрощение развертывания и настройки.

---

### 3. Очистка кодовой базы (Выполнено)

-   **[X] Задача:** Удалить неиспользуемый код, зависимости и старые скрипты.
-   **[X] Действие:**
    -   Удалить функцию `get_articles_from_rss` из `src/parser.py`.
    -   Удалить зависимость `feedparser` из `requirements.txt`.
    -   Удалить неиспользуемые скрипты из папки `scripts/`.
-   **[X] Обоснование:** Уменьшение объема кодовой базы, облегчение ее понимания и поддержки.

---

### 4. Улучшение документации (Выполнено)

-   **[X] Задача:** Сделать документацию по развертыванию более четкой и подробной.
-   **[X] Действие:**
    -   Переписать `DEPLOYMENT.md`, добавив пошаговые инструкции, примеры и раздел по устранению неполадок.
-   **[X] Обоснование:** Упрощение процесса установки и настройки для новых пользователей.